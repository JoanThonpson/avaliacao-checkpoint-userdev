import 'package:flutter/material.dart';

class AppColors {
  // Cores principais do Figma (geek/neon)
  static const Color primary = Color(0xFF780BF7);     // Roxo principal
  static const Color primaryDark = Color(0xFF430091); // Roxo escuro
  static const Color accentPink = Color(0xFFFF55DF);  // Rosa neon
  static const Color accentGreen = Color(0xFF8FFF24); // Verde limão
  static const Color backgroundDark = Color(0xFF090129); // Fundo dark
  static const Color surface = Color(0xFF1A1A2E);     // Superfície escura

  // Texto
  static const Color textPrimary = Colors.white;
  static const Color textSecondary = Color(0xFFB0B0C0);

  // Outros
  static const Color divider = Color(0xFF2A2A40);
  static const Color success = Color(0xFF8FFF24);
  static const Color error = Color(0xFFFF5555);
}